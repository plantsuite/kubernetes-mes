apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: workflows-studio
spec:
  hosts:
    - workflows-studio.plantsuite.svc.cluster.local
  gateways:
    - istio-ingress/gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            port:
              number: 80
            host: workflows-studio